
/*
 * 
 * Default Admin password is               hazir1234
 * Default internet wifi ssid is           ptcl
 * Default internet wifi passward is       khird2300
 * Default Hz ssid is                      Hazir
 * Default Hz password is                  hazir
 * Master reset key is                     4
 * 
 * 
 * SD Card Files
 * EmployeeData.txt    --> saves the Employee's ID against his name
 * Enrollment.txt      --> saves all the logs of succesfull fingerprint search
 * Remaining.txt       --> saves the logs of fingerprint search that has not been updated to web
 */

 
